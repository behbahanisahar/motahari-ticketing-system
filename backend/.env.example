# Optional — defaults work for local Docker run.
# Copy to .env and customize for production.

PORT=4000
CORS_ORIGIN=http://localhost:8080
DATABASE_URL=postgres://ticketing_user:ticketing_pass@localhost:5432/ticketing
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=12h
COOKIE_SECURE=false
